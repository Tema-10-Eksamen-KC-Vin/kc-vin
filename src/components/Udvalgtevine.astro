---
const { title } = Astro.props;
---

<div class="flex">
  <h2>{title}</h2>
  <slot />
</div>

<style>
  .flex {
    justify-items: center;
    grid-column: 2 / 3;
    grid-row: 1 / 3;
    display: flex;
    flex-direction: column;
    gap: 30px;
  }
</style>
