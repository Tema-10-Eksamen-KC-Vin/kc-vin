---
import Navbar from "./Navbar.astro";
import { Image } from "astro:assets";
import Eventhero from "../assets/eventhero.png";
---

<header>
  <div class="headergrid">
    <nav class="navbar">
      <Navbar />
    </nav>
    <div class="redoverlay">
      <div class="grid">
        <div class="headercontent">
          <h1>Nye vinsmagnings events</h1>
          <p>Leder du efter en ny oplevelse, meld dig til vores vinsmagninger og få en uforglemmelig aften med unikke vin.</p>
        </div>
      </div>
    </div>
    <Image src={Eventhero} alt="" />
  </div>
</header>

<style>
  .redoverlay {
    background: linear-gradient(180deg, rgba(65, 20, 21, 0) 0%, rgba(65, 20, 21, 1) 100%);
    width: 100%;
    height: 500px;
    grid-row: 1 / 2;
    grid-column: 1 / 2;
    z-index: 1;
    display: grid;
    grid-template-columns: 1fr;
    grid-template-rows: 1fr;
  }
  .headergrid {
    display: grid;
    grid-template-columns: 1fr;
    grid-template-rows: 1fr;
    position: relative;
    max-height: 100vh;
    overflow: hidden;
  }
  .navbar {
    grid-column: 1 / 2;
    grid-row: 1 / 2;
  }
  img {
    grid-row: 1 / 2;
    grid-column: 1 / 2;
    width: 100%;
    height: 500px;
    object-fit: cover;
  }
  .headercontent {
    color: var(--color-white);
    z-index: 2;
    grid-row: 1 / 2;
    grid-column: 1 / 2;
    align-content: end;
    padding: 50px 20px;
  }
  .grid {
    max-width: 1250px;
    margin: auto;
    display: grid;
    grid-template-columns: 1fr;
    grid-template-rows: 1fr;
    grid-row: 1 / 2;
    grid-column: 1 / 2;
    width: 100%;
    height: 100%;
  }
</style>
